<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.leandrojara.dbv_finnance">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="firebase_crashlytics_collection_enabled"
                android:value="false" />
        </activity>
        <activity android:name=".activity.CadastroUsuarioActivity" />
        <activity android:name=".activity.MultiRolesActivity" />
        <activity android:name=".activity.AdministradorActivity" />
        <activity android:name=".activity.DiretorActivity" />
        <activity android:name=".activity.TesoureiroActivity" />
        <activity android:name=".activity.ResponsavelActivity"></activity>
    </application>

</manifest>